import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonSkill: {
    marginTop: 10,
    backgroundColor: '#1f1e25',
    padding: 15,
    borderRadius: 15,
    alignItems: 'center',
  },

  textSkills: {
    color: '#fff',
    fontSize: 16,
  },
});
