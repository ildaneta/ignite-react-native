import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%'
  },

  containerWithoutError: {
    flex: 1,
    marginBottom: 56
  },

  containerWithError: {
    flex: 1,
    marginBottom: 60,
  },
}) 